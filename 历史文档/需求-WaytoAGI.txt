编写一个Python脚本goWTA.py，把WaytoAGI知识库特定文档中的URL信息提取出来，存放在飞书多维表格中。
WaytoAGI文档地址等如下信息需存放在配置文件中。
    近期文档：https://waytoagi.feishu.cn/wiki/QPe5w5g7UisbEkkow8XcDmOpn8e。
        该文档“近7日更新日志”章节部分，提取其内容中含有的URL地址。
    历史归档：https://waytoagi.feishu.cn/wiki/FjiOwWp2giA7hRk6jjfcPioCnAc。
        该文档内，提取其内容中含有的URL地址。
    结果需写入如下飞书多维表格文件的特定表格中：
        app_token: LOBswegeNiQIf4kPDAtcJDbhnTg
        table_id: tblkFLElRj4lZvbb
    解析日期范围，示例：从20260221到最新。
    已解析完成的最新日期。
先解析20260221日之前的内容。部分日期的文件在历史文档中，部分日期的文件在近期文档中。
表格各列的内容要求如下：
1) "链接"列。页面中的URL网址。需按照遵循以往方式进行截断后。
2) “标题”列。
  - 基于URL链接，获取其文章的标题。如果没有标题，则选取文档中与此URL对应的文字作为标题，并在两端分别放入圆括弧（中文全角字符）。
3) “日期”列。格式示例：20250502。
  - 基于URL链接，获取其文章的更新日期。
4) “星期”列。根据日期进行计算。格式示例：周三
5) “来源”列。遵循url_parser.py的解析方式进行记录。
6) "大本营精选"列。记录为“WaytoAGI”。
写入顺序按该链接在文档中归属的日期进行排序，同一日期的，遵循URL出现在历史文档中的顺序。

此工作分为三阶段进行：
1) 从两个文档中解析出日期范围内的全部URL，写入临时文件中。参照之前goAIPM.py的经验，不应有遗漏。
2) 将全部URL链接进行内容解析，写入临时csv文件中。无法完成解析的文档，需将错误信息参照goAIPM.py程序的经验，记录在专门的log文件中（csv格式）。
3) 模拟结果完成后，再写入到多维表格中。
4) 补充一个机制，对于未能解析的文件，请在全部解析轮询完成后，再尝试一遍。避免因为网络访问因素而导致遗漏。  

参数控制约定如下：
--his，解析制定日期范围的文字内容。
--update，基于之前记录的已解析完成日期，解析该日期之后的日期的文字内容。

请你理解一下我的这个需求，和我讨论后再开始工作。

